export enum CollectionName {
  AUTH = 'auth',
  AUTH_CODE = 'auth_code',
  TOKEN = 'token',
  USER = 'user',
}

export enum ModelName {
  AUTH = 'Auth',
  AUTH_CODE = 'AuthCode',
  TOKEN = 'Token',
  USER = 'User',
}
